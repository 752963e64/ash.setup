#!/usr/bin/env ash

[[ "$1" ]] || return 1

case "$1" in
  start)
    modprobe -v kvm
    lsmod | grep kvm
    chmod o-rwx /dev/kvm
  ;;
  stop)
    rmmod -v kvm
  ;;
  *)
    echo "Usage: ${0##*/} start|stop"
  ;;
esac


