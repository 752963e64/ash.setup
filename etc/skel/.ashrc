# ~/.ashrc

# default permissions for newly created dir/file
# umask 027

# shows up your todos & notices each times you open up a shell.
[ -f ~/.todo.txt ] && { cat ~/.todo.txt; }
# [ -f ~/.notice.txt ] && { cat ~/.notice.txt; }

