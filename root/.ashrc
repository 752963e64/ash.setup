# ~/.ashrc

# stop here when invocation isn't interactive.
case $- in
  *i*) ;;
  *)
    return
  ;;
esac

# ensure root doesn't leak information
# when creating file/dir, however this may be problematic
# with package manager that follows the mask...
# umask 027

